def get(build_ele):
    widme = build_ele.widme.value
    widl = build_ele.widl.value
    cutll = build_ele.cutll.value
    widm = build_ele.widm.value
    hil = build_ele.hil.value
    him = build_ele.him.value
    widt = build_ele.widt.value
    hit = build_ele.hit.value
    len = build_ele.len.value
    liti = build_ele.liti.value
    i = build_ele.i.value
    return [widme, widl, cutll, widm, hil, him, widt, hit, len, liti, i]
